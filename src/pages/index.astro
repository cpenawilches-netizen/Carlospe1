---
import '../styles/global.css';

const categories = [
  {
    name: 'Botas de seguridad',
    detail: 'Calzado industrial con puntera, suelas antideslizantes y proteccion diaria.',
    items: ['Puntera de seguridad', 'Antideslizantes', 'Dielectricas'],
  },
  {
    name: 'Botas de caucho',
    detail: 'Botas impermeables para campo, aseo, alimentos y trabajos con humedad.',
    items: ['Cana alta', 'Uso agroindustrial', 'Aseo y alimentos'],
  },
  {
    name: 'Zueco dotacion',
    detail: 'Opciones comodas para cocina, salud, servicios, colegios e instituciones.',
    items: [
      'Zapatos Antideslizantes',
      'Calzado Institucional',
      'Calzado Colegial y de Trabajo',
      'Zapatos para Cocina, Salud y Servicios',
    ],
  },
];

const footwearTypes = [
  {
    id: 'bota-seguridad',
    name: 'Bota de seguridad',
    summary: 'Proteccion industrial con puntera, suela antideslizante y materiales resistentes.',
    products: [
      {
        name: 'Seguridad Pro S1',
        tag: 'Puntera',
        price: '$249.900',
        color: 'Negro',
        spec: 'Ideal para obra, bodega y planta',
      },
      {
        name: 'Terreno Max',
        tag: 'Antideslizante',
        price: '$289.900',
        color: 'Cafe',
        spec: 'Suela de alto agarre y cuello acolchado',
      },
      {
        name: 'Metal Free',
        tag: 'Ligera',
        price: '$269.900',
        color: 'Gris / negro',
        spec: 'Proteccion sin componentes metalicos',
      },
    ],
  },
  {
    id: 'bota-caucho',
    name: 'Bota de caucho',
    summary: 'Botas impermeables para lluvia, campo, aseo, alimentos y trabajo pesado.',
    products: [
      {
        name: 'Caucho Alta',
        tag: 'Impermeable',
        price: '$119.900',
        color: 'Negro',
        spec: 'Cana alta para humedad y lodo',
      },
      {
        name: 'Agro Flex',
        tag: 'Campo',
        price: '$139.900',
        color: 'Verde oliva',
        spec: 'Flexible para jornadas largas',
      },
      {
        name: 'Food Safe',
        tag: 'Aseo',
        price: '$129.900',
        color: 'Blanco',
        spec: 'Pensada para alimentos y limpieza',
      },
    ],
  },
  {
    id: 'zueco-dotacion',
    name: 'Zueco dotacion',
    summary: 'Calzado liviano, facil de limpiar y comodo para instituciones, salud y servicios.',
    products: [
      {
        name: 'Zapatos Antideslizantes',
        tag: 'Agarre',
        price: 'Cotizar',
        color: 'Negro / blanco',
        spec: 'Para jornadas en superficies lisas o humedas',
      },
      {
        name: 'Calzado Institucional',
        tag: 'Dotacion',
        price: 'Cotizar',
        color: 'Segun pedido',
        spec: 'Modelos para equipos de trabajo e instituciones',
      },
      {
        name: 'Calzado Colegial y de Trabajo',
        tag: 'Resistente',
        price: 'Cotizar',
        color: 'Negro',
        spec: 'Uso diario con horma comoda y materiales durables',
      },
      {
        name: 'Zapatos para Cocina, Salud y Servicios',
        tag: 'Servicio',
        price: 'Cotizar',
        color: 'Blanco / negro',
        spec: 'Faciles de limpiar y pensados para largas jornadas',
      },
    ],
  },
];

const supabaseUrl = import.meta.env.PUBLIC_SUPABASE_URL;
const supabaseAnonKey = import.meta.env.PUBLIC_SUPABASE_ANON_KEY;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="InduProt: tienda colombiana de calzado industrial, botas de seguridad y botas de caucho."
    />
    <title>InduProt | Calzado industrial y de proteccion</title>
    <link rel="icon" type="image/png" href="/images/induprot-icon.png" />
  </head>
  <body>
    <header class="topbar">
      <nav class="category-menu" aria-label="Categorias de calzado">
        <div class="menu-item">
          <a class="menu-trigger" href="#productos">Productos</a>
          <div class="submenu product-submenu">
            {categories.map((category) => (
              <div class="submenu-group">
              <p>{category.name}</p>
              {category.items.map((item) => (
                <a href="#productos">{item}</a>
              ))}
              </div>
            ))}
          </div>
        </div>
      </nav>
      <a class="brand" href="/" aria-label="Inicio">
        <img src="/images/induprot-logo.png" alt="InduProt" />
      </a>
      <div class="topbar-actions">
        <a class="top-icon" href="#productos" aria-label="Buscar productos">
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <circle cx="11" cy="11" r="7"></circle>
            <path d="m16.5 16.5 4 4"></path>
          </svg>
        </a>
        <a class="top-icon" href="/configurador" aria-label="Mi cuenta">
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <path d="M20 21a8 8 0 0 0-16 0"></path>
            <circle cx="12" cy="7" r="4"></circle>
          </svg>
        </a>
        <a class="top-icon" href="#contacto" aria-label="Carrito de compra">
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <circle cx="9" cy="21" r="1"></circle>
            <circle cx="20" cy="21" r="1"></circle>
            <path d="M1 1h4l2.7 13.4a2 2 0 0 0 2 1.6h8.7a2 2 0 0 0 2-1.6L22 6H6"></path>
          </svg>
        </a>
      </div>
    </header>

    <main>
      <section class="hero">
        <img src="/images/hero-calzado.png" alt="Coleccion de zapatos en exhibicion comercial" />
        <div class="hero-overlay">
          <p class="eyebrow">Coleccion 2026</p>
          <h1>Calzado para caminar, trabajar y moverse mejor.</h1>
          <p>
            Una vitrina moderna para vender tenis, cuero, botas y sandalias con despacho en
            Colombia.
          </p>
          <div class="hero-actions">
            <a class="button primary" href="#productos">Ver destacados</a>
            <a class="button secondary" href="#contacto">Cotizar pedido</a>
          </div>
        </div>
      </section>

      <section class="promo-strip" aria-label="Beneficios">
        <span>Envios nacionales</span>
        <span>Cambios faciles</span>
        <span>Pagos seguros</span>
        <span>Asesoria por WhatsApp</span>
      </section>

      <section id="categorias" class="section">
        <div class="section-heading">
          <p class="eyebrow">Compra por categoria</p>
          <h2>Una entrada clara para cada cliente</h2>
        </div>
        <div class="category-grid">
          {categories.map((category) => (
            <article class="category-card">
              <h3>{category.name}</h3>
              <p>{category.detail}</p>
              <a href="#productos">Explorar</a>
            </article>
          ))}
        </div>
      </section>

      <section id="productos" class="section product-section">
        <div class="section-heading">
          <p class="eyebrow">Catalogo por tipo</p>
          <h2>Elige el calzado que necesita tu trabajo</h2>
        </div>

        <div
          class="type-tabs"
          id="catalogo-calzado"
          data-supabase-url={supabaseUrl}
          data-supabase-anon-key={supabaseAnonKey}
        >
          {footwearTypes.map((type, index) => (
            <input
              class="tab-input"
              type="radio"
              name="tipo-calzado"
              id={type.id}
              checked={index === 0}
            />
          ))}

          <div class="tab-list" role="tablist" aria-label="Tipos de calzado">
            {footwearTypes.map((type) => (
              <label class="tab-label" for={type.id}>{type.name}</label>
            ))}
          </div>

          <div class="type-panels">
            {footwearTypes.map((type) => (
              <article class="type-panel">
                <div class="type-intro">
                  <h3>{type.name}</h3>
                  <p>{type.summary}</p>
                  <a class="text-link" href="#contacto">Cotizar este tipo</a>
                </div>
                <div class="product-grid compact">
                  {type.products.map((product) => (
                    <a class="product-card" href={`/producto?slug=${type.id}-${product.name.toLowerCase().replaceAll(' ', '-')}`}>
                      <div class="shoe-visual" aria-hidden="true">
                        <span></span>
                      </div>
                      <div class="product-meta">
                        <span>{product.tag}</span>
                        <h3>{product.name}</h3>
                        <p>{product.color}</p>
                        <p>{product.spec}</p>
                        <strong>{product.price}</strong>
                      </div>
                    </a>
                  ))}
                </div>
              </article>
            ))}
          </div>
        </div>
      </section>

      <section id="ventajas" class="feature-band">
        <div>
          <p class="eyebrow">Pensado para ecommerce</p>
          <h2>Rapido, editable y listo para crecer</h2>
        </div>
        <div class="features">
          <article>
            <h3>Astro</h3>
            <p>Paginas rapidas, buen SEO y componentes faciles de mantener.</p>
          </article>
          <article>
            <h3>GitHub</h3>
            <p>Control de cambios para trabajar desde VS Code sin perder historial.</p>
          </article>
          <article>
            <h3>Cloudflare</h3>
            <p>Deploy automatico cada vez que se sube una mejora al repositorio.</p>
          </article>
        </div>
      </section>

      <section id="contacto" class="contact-section">
        <div>
          <p class="eyebrow">Siguiente paso</p>
          <h2>Conectemos catalogo, pagos y WhatsApp</h2>
          <p>
            Esta primera version deja la base visual lista. Luego podemos agregar productos reales,
            filtros, carrito, CMS o integracion con inventario.
          </p>
        </div>
        <a class="button primary" href="mailto:ventas@tumarca.com">ventas@tumarca.com</a>
      </section>
    </main>

    <script>
      const catalog = document.querySelector('#catalogo-calzado');
      const supabaseUrl = catalog?.dataset.supabaseUrl;
      const supabaseAnonKey = catalog?.dataset.supabaseAnonKey;

      const formatPrice = (value) => {
        if (value === null || value === undefined || value === '') {
          return 'Cotizar';
        }

        return new Intl.NumberFormat('es-CO', {
          style: 'currency',
          currency: 'COP',
          maximumFractionDigits: 0,
        }).format(Number(value));
      };

      const renderCatalog = (types) => {
        if (!catalog || !types.length) return;

        catalog.innerHTML = `
          <div class="tab-list" role="tablist" aria-label="Tipos de calzado">
            ${types
              .map(
                (type, index) => `
                  <button
                    class="tab-label ${index === 0 ? 'is-active' : ''}"
                    type="button"
                    data-tab-target="${type.slug}"
                  >
                    ${type.nombre}
                  </button>
                `,
              )
              .join('')}
          </div>

          <div class="type-panels">
            ${types
              .map(
                (type, index) => `
                  <article class="type-panel ${index === 0 ? 'is-active' : ''}" data-tab-panel="${type.slug}">
                    <div class="type-intro">
                      <h3>${type.nombre}</h3>
                      <p>${type.descripcion ?? ''}</p>
                      <a class="text-link" href="#contacto">Cotizar este tipo</a>
                    </div>
                    <div class="product-grid compact">
                      ${type.productos
                        .map(
                          (product) => `
                            <a class="product-card" href="/producto?slug=${encodeURIComponent(product.slug)}">
                              <div class="shoe-visual">
                                ${
                                  product.imagen_url
                                    ? `<img class="product-image primary-image" src="${product.imagen_url}" alt="${product.nombre}" loading="lazy" />
                                      ${
                                        product.imagen_hover_url
                                          ? `<img class="product-image hover-image" src="${product.imagen_hover_url}" alt="Otra vista de ${product.nombre}" loading="lazy" />`
                                          : ''
                                      }`
                                    : '<span></span>'
                                }
                              </div>
                              <div class="product-meta">
                                <span>${product.etiqueta ?? 'Producto'}</span>
                                <h3>${product.nombre}</h3>
                                <p>${product.color ?? ''}</p>
                                <p>${product.descripcion ?? ''}</p>
                                <strong>${formatPrice(product.precio)}</strong>
                              </div>
                            </a>
                          `,
                        )
                        .join('')}
                    </div>
                  </article>
                `,
              )
              .join('')}
          </div>
        `;

        catalog.querySelectorAll('[data-tab-target]').forEach((tab) => {
          tab.addEventListener('click', () => {
            const target = tab.getAttribute('data-tab-target');

            catalog.querySelectorAll('[data-tab-target]').forEach((item) => {
              item.classList.toggle('is-active', item === tab);
            });

            catalog.querySelectorAll('[data-tab-panel]').forEach((panel) => {
              panel.classList.toggle('is-active', panel.getAttribute('data-tab-panel') === target);
            });
          });
        });
      };

      const loadCatalogFromSupabase = async () => {
        if (
          !catalog ||
          !supabaseUrl ||
          !supabaseAnonKey ||
          supabaseUrl === 'undefined' ||
          supabaseAnonKey === 'undefined'
        ) {
          return;
        }

        const url = new URL('/rest/v1/tipos_calzado', supabaseUrl);
        url.searchParams.set('select', 'id,nombre,slug,descripcion,orden,productos(*)');
        url.searchParams.set('activo', 'eq.true');
        url.searchParams.set('productos.disponible', 'eq.true');
        url.searchParams.set('order', 'orden.asc');

        const response = await fetch(url, {
          headers: {
            apikey: supabaseAnonKey,
            Authorization: `Bearer ${supabaseAnonKey}`,
          },
        });

        if (!response.ok) {
          throw new Error('No se pudo cargar el catalogo desde Supabase');
        }

        const types = await response.json();
        const orderedTypes = types.map((type) => ({
          ...type,
          productos: [...(type.productos ?? [])].sort((a, b) => (a.orden ?? 0) - (b.orden ?? 0)),
        }));

        renderCatalog(orderedTypes);
      };

      loadCatalogFromSupabase().catch((error) => {
        console.warn(error);
      });
    </script>
  </body>
</html>
